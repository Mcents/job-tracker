<h1>All Categories</h1>

<% @category.each do |cat| %>

  <h1><%= link_to cat.title, category_path(cat) %></h1>
  <%= link_to "Edit", edit_category_path(cat) %>
  <%= link_to "Delete", category_path(cat), method: :delete  %>

<% end %>
